# Pinned for reproducibility. Default transport is OpenRouter; anthropic/* ids can optionally
# route through the native Anthropic SDK instead (harness/backends.py's AnthropicNative*
# backends, harness/anthropic_batch.py's Batch API path) — see the root README's transport note.
openai>=2.28,<3       # the OpenRouter client (OpenAI-compatible chat-completions API)
anthropic>=0.85,<1    # native Anthropic transport + Batch API (anthropic/* ids only)
numpy>=1.26,<3        # paired bootstrap CIs (harness/stats.py)
scipy>=1.11,<2        # paired t-tests (harness/stats.py)
matplotlib>=3.8,<4    # figure rendering
